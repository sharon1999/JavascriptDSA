function swap(x, y) {
    var t = x;
    x = y;
    y = t;
    return [x, y];
}

function insertionSort(arr){
   for(let i = 0;i<arr.length;i++){
        let nti = arr[i+1]
        let se=arr[i]
        if(se>nti){
            arr[i]
        }
   }
  }
  

const res = insertionSort([8,20,-2,4,6])
console.log(res)
  
  